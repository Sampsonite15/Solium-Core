The root location for the Solium project for git is: C:\Users\andy\PycharmProjects\Solium
To move git to the proper folder location, use the following bash command: cd /c/Users/andy/PycharmProjects/Solium

Useful Bash Commands:
    git add filename.txt --Stages a file with the name "filename.txt"
    git add . --Stages ALL files in the root directory
    git commit -m "Describe what changed, like 'Add dedupe script and update Dev Notes'" --This commits changes to files
    git push origin Solium-Core --Pushes all committed files to Solium-Core on GitHub
    git status --Checks for items that are staged, committed, or untracked
    mkdir -p [versions/v3.0 \] --Creates a file directory folder
    git mv "CENOS_Alert v3.0.py" versions/v3.0/ --moves a file (in quotes) to the designated folder.

